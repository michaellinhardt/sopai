# Workflow Context

## Request
Process Ralph transcript through RAG generation, fact extraction, parallel fact verification, and final report generation.

## Purpose
Generate a comprehensive, fact-checked report on Ralph based on transcript analysis.

## Method
1. Generate RAG from `./request/ralph.transcript.md` using `agt-rag-generator`
2. Extract all facts from RAG into a list using `agt-technical-lead`
3. Verify each fact in parallel using `agt-technical-lead` with web searches
4. Compile final report with all findings using `agt-technical-lead`

## Acceptance Criteria
- RAG output at `./request/ralph.rag.md`
- Facts list at workflow folder `facts.list.md`
- Individual fact check files at `[snakeCaseFactShortName].check.md`
- Final report at `./request/ralph.report.md`
- Any exaggerated or incorrect facts clearly noted in report

## State Log
- Workflow initialized
